[project]
name = "localized-finetuning"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.26.0",
    "python-dotenv>=1.1.0",
    "matplotlib>=3.10.8",
    "datasets>=4.8.4",
    "huggingface-hub>=1.6.0",
]

[project.optional-dependencies]
local = [
    "torch>=2.0.0",
    "transformers>=4.40.0",
    "peft>=0.11.0",
]
openweights = [
    "openweights>=0.11.0",
]
all = [
    "torch>=2.0.0",
    "transformers>=4.40.0",
    "peft>=0.11.0",
    "openweights>=0.11.0",
]

[tool.uv.sources]
openweights = { git = "https://github.com/longtermrisk/openweights.git" }
