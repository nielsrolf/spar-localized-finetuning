# Chart Generators

Plotting scripts for eval analysis charts.

Conventions:

- Read CSV inputs from `../../csv_results/`.
- Write PNG charts to `../generated_charts/png/`.
- Write interactive HTML charts to `../generated_charts/html/` when useful.
- Keep chart style consistent across scripts: Monet palette, STIX font, warm canvas background.
